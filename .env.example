# SQLite (default — no setup required)
DATABASE_URL="file:./dev.db"

# PostgreSQL (set this instead of the line above to use Postgres)
# DATABASE_URL="postgresql://user:password@localhost:5432/resurrect"

JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters"
ANTHROPIC_API_KEY=""
